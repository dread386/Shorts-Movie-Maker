# Shorts Movie Maker — 改修およびアプリ内 字幕エディタ完了報告

## 🛠️ 今回追加された最新機能

### 1. アプリ内 字幕（SRT）エディタ＆即時再生成 ✏️🔄
- 生成された各ショート動画カードに **「✏️ 字幕編集」** ボタンを設置しました。
- クリックするとモーダル画面が開き、タイムスタンプごとの字幕テキストをブラウザ上で直接修正・行追加・削除できます。
- **「🔄 修正して動画に再反映 (即時)」** ボタンを押すと、そのクリップ（15秒前後）だけを**数秒で再レンダリング**し、動画プレイヤーとSRTファイルが即座に最新のものに更新されます。
- 外部ソフト（Premiere Pro等）を開くことなく、Whisperの聞き間違いや誤字を直して完成動画をダウンロードできます。

### 2. ショート動画の長さ（10秒 / 15秒 / 20秒 / 30秒）選択
- ご自身のデータ（平均視聴時間15秒）に最適化した尺で、2分前後の動画から指定したクリップ数（4〜6本）を重複なく確実に切り出せます。

### 3. APIキーの3つ自動ローテーション
- `/Volumes/DTM/applications/APIキーリスト.txt` の 3 つの Gemini API キーを自動読み込みし、制限（Rate Limit）が来たら即座に次のキーへ自動切替します。

### 4. Whisper 幻覚除去 & ギター手元クロップ
- 「作詞・作曲」などの誤字幕を完全除去。
- ギター動画向けに「背景ぼかしパディング (`blur_pad`)」と「左手・指板フォーカス (`left_crop`)」を搭載。

---

## 🎬 起動方法
Finder から `/Volumes/DTM/applications/Shorts Movie Maker/起動.command` をダブルクリックして起動してください。
ブラウザ（`http://localhost:5175`）が自動で開きます。
