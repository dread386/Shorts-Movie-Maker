# 🎬 Shorts Movie Maker — 完全取扱説明書 (User Manual)

> **バージョン**: v1.3.1  
> **対応OS**: macOS (Apple Silicon / Intel)  
> **開発環境**: Python 3.11+ / Flask / FFmpeg / faster-whisper / Style-BERT-VITS2 / edge-tts / Google Gemini API  

---

## 📖 目次

1. [Shorts Movie Maker とは？（概要と特徴）](#1-shorts-movie-maker-とは概要と特徴)
2. [動作原理とシステム構成図](#2-動作原理とシステム構成図)
3. [主要機能の詳細解説](#3-主要機能の詳細解説)
   - [① 8区画グリッド分割（A〜H）× マルチスロット配置](#-8区画グリッド分割ah--マルチスロット配置)
   - [② AI ハイライト自動選定 (Gemini API 連携)](#-ai-ハイライト自動選定-gemini-api-連携)
   - [③ 音声フック読み上げ ＆ スマートダッキング](#-音声フック読み上げ--スマートダッキング)
   - [④ スマホ最適化・超迫力ワイドタイトルバナー](#-スマホ最適化超迫力ワイドタイトルバナー)
   - [⑤ 9:16 専用高CTRサムネイル自動生成](#-916-専用高ctrサムネイル自動生成)
   - [⑥ アプリ内 字幕＆バナーエディタ（即時再反映）](#-アプリ内-字幕バナーエディタ即時再反映)
   - [⑦ ワンクリック・ストレージ容量解放（一括クリーンアップ）](#-ワンクリックストレージ容量解放一括クリーンアップ)
   - [⑧ ワンクリック・アプリ停止 ＆ ターミナル終了](#-ワンクリックアプリ停止--ターミナル終了)
4. [推奨ワークフロー（ステップ・バイ・ステップ操作手順）](#4-推奨ワークフローステップバイステップ操作手順)
5. [各パラメータ・設定項目の詳細](#5-各パラメータ設定項目の詳細)
6. [トラブルシューティング ＆ よくある質問 (FAQ)](#6-トラブルシューティング--よくある質問-faq)

---

## 1. Shorts Movie Maker とは？（概要と特徴）

**Shorts Movie Maker** は、YouTube等に投稿された長尺の横動画（16:9）から、AIが視聴者の目を引く見どころ（サビやトークの山場）を自動検出し、**スマホ専用の縦型ショート動画（9:16）＋高CTRアイキャッチサムネイル＋音声フック（ナレーション）＋テロップ字幕付きで一括全自動生成**するローカルWebアプリケーションです。

### 🌟 従来の編集作業との違い
| 項目 | 従来の手作業（Premiere / CapCut等） | Shorts Movie Maker |
| :--- | :--- | :--- |
| **作業時間** | 1本のショート作成に 30分〜1時間 | **動画を放り込んでボタンを押すだけ (数分で複数本一括生成)** |
| **見どころ選定** | 動画全体を目視でシークして探す | **Whisper文字起こし ＋ Gemini AI が自動選定** |
| **画面構成** | 縦横比の切り抜き・配置を手動調整 | **8区画グリッド（A〜H）からワンクリックでマルチカメラ風合成** |
| **ナレーション** | 音声ソフトで書き出して音量手動調整 | **冒頭タイトルを自動読み上げ ＋ 演奏音量を自動ダッキング** |
| **サムネイル** | Photoshop等で別枠作成 | **動画と同時に 1080x1920 高解像度 PNG サムネイルを自動生成** |
| **データ管理** | 中間ファイルが散らかり容量を圧迫 | **ワンクリックで一時ファイルを全削除して容量ゼロに復帰** |

---

## 2. 動作原理とシステム構成図

```
[横動画 16:9 ファイル]
       │
       ▼
[FFmpeg: 映像・音声抽出 & 0.05秒実写サムネイル生成]
       │
       ├────────────────────────────────────────┐
       ▼                                        ▼
[faster-whisper: 文字起こし & タイムスタンプ]   [8区画グリッド分割エンジン]
       │                                        │
       ▼                                        │
[Gemini 1.5: ハイライトシーン & フック選定]    │
       │                                        │
       ├───────────────────┐                    │
       ▼                   ▼                    │
[縦型マルチスロット合成] [TTS音声合成 (ダッキング)]     │
       │                   │                    │
       └─────────┬─────────┘                    │
                 ▼                              │
    [字幕テロップ & ワイドバナー焼き込み]        │
                 │                              │
                 ▼                              ▼
    [1080x1920 完成ショート動画 MP4]   [9:16 高解像度サムネイル PNG]
                 │                              │
                 └──────────────┬───────────────┘
                                ▼
              [📦 一括ZIPダウンロード & 容量解放]
```

---

## 3. 主要機能の詳細解説

### 📐 8区画グリッド分割（A〜H）× マルチスロット配置
元の横動画（16:9 / 1920x1080）を **横4列 × 縦2段 の計8区画（各480x540）** に座標定義しています。

```
┌─────────────────────────────────────────────────────────────┐
│                      横動画 (16:9)                          │
│ ┌──────────────┬──────────────┬──────────────┬────────────┐ │
│ │ A: 左手上    │ B: 中左上    │ C: 中右上(顔)│ D: 右上    │ │
│ ├──────────────┼──────────────┼──────────────┼────────────┤ │
│ │ E: 左手下    │ F: 手元(ピク)│ G: アンプ    │ H: 右下    │ │
│ └──────────────┴──────────────┴──────────────┴────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

* **実写サムネイルオーバーレイ**: 動画を選択した瞬間に、実際の演奏フレームの上に黄色いグリッド線と A〜H のラベルが重なって表示されます。
* **多彩な縦動画レイアウト**:
  * **上下2分割 (上/下)**: ギター演奏動画の王道（上: A左手ネック / 下: Fピッキング手元）。
  * **上中下3分割 (上/中/下)**: 上段に顔（C）、中段に左手（A）、下段に手元（F）。
  * **2×2 タイル (4画面)**: バンド演奏や複数アングルの同時表示。
  * **4段分割 (上/中上/中下/下)**: 超情報密度の高いマルチレイアウト。
  * **左右2分割 (左/右)**: 比較動画やツインギター等に最適。
* **ワイド・縦長区画の網羅**:
  * `BCD`（上段右3/4・A以外）、`ABCD`（上段全幅）、`EFGH`（下段全幅）、`AE`（左端縦長）、`BF`（中左縦長）なども自由に選択可能です。

---

### 🤖 AI ハイライト自動選定 (Gemini API 連携)
* Whisperで生成したタイムラインをもとに、Google Gemini API が「視聴維持率が高くなる盛り上がりポイント」「サビ」「決定的なフレーズ」を検出します。
* **3つのAPIキー自動ローテーション**: レート制限（429エラー）が発生しても、バックアップキーに自動で切り替えて処理を継続します。
* **ルールベース自動フォールバック**: APIキーが未設定、または通信不能の場合でも、音声密度や発話間隔から自動的に最適なシーンを切り出します。

---

### 🎙️ 音声フック読み上げ ＆ スマートダッキング
ショート動画の冒頭 0〜3秒で視聴者を惹きつける「音声フック」を自動合成します。

* **Style-BERT-VITS2 (ローカルAI音声)**:
  * 自分の声モデル（`my_voice`）やキャラクターボイスで、自然で感情豊かな音声を合成。
* **edge-tts (高品質クラウド音声)**:
  * `Keita`（男性・親しみやすい）、`Naoki`（男性・落ち着き）、`Nanami`（女性・自然）、`Aoi`（女性・高音）など、高速かつ安定した音声を合成。
* **🔊 声を試聴ボタン**:
  * メニュー横のボタンを押すと、**「ショート動画を自動生成します」** というサンプル音声が 0.3秒で即座に再生され、耳で確認しながら声を選べます。
* **スマートオーディオダッキング**:
  * 音声が流れている間だけ、元の演奏・BGMの音量を約 **30% に自動減衰**させ、音声終了後にスムーズに 100% に復帰させます。

---

### 💡 スマホ最適化・超迫力ワイドタイトルバナー
スマホの小さな画面（6.1インチ）でスクロールされた際、0.5秒で文字を読ませるための**極太ワイドバナープレート**を自動描画します。

```
┌────────────────────────────────────────────────────────────┐
│ ┌────────────────────────────────────────────────────────┐ │
│ │  ナメていると余裕で置いていかれるソロ                  │ │ ← 幅980〜1020px
│ └────────────────────────────────────────────────────────┘ │   文字サイズ 76〜84px
│                                                            │
│                    (ギター演奏画面)                        │
└────────────────────────────────────────────────────────────┘
```

* **画面横幅いっぱいのワイド帯（980px〜1020px）**: 文字数に関係なく画面上部をドカンと占有。
* **文字サイズ選択**:
  * **超特大 (`84px`)**: スマホで一番目立つバズ黄金サイズ！
  * **特大 (`76px` / ★推奨)**: 2行フックが迫力満点に収まる黄金比率。
  * **大 (`68px`)**: 短文・3行向け。
  * **中 (`58px`)**: 解説や長文向け。
* **カラープリセット**: `黄帯 × 黒背景`（YouTube推奨・高CTR）、`赤帯 × 黒背景`（緊急・インパクト）、`シンプル白枠`。

---

### 🖼️ 9:16 専用高CTRサムネイル自動生成
* 動画の中から最も映える代表フレームを自動抽出し、極太バナーとテロップを合成した **1080x1920 高解像度 PNG サムネイル** を動画とセットで自動生成します。
* YouTubeショートやTikTok、Instagramリールのカバー画像としてそのまま設定できます。

---

### ✏️ アプリ内 字幕＆バナーエディタ（即時再反映）
生成された各クリップの「✏️ 字幕＆バナー編集」ボタンから、ブラウザ上で直感的に修正が可能です。

* **バナー文の書き換え**: タイトルやフック文を自由に変更。
* **文字サイズ・声の再選択**: バナーサイズや読み上げ音声を変更可能（ここでも試聴可能）。
* **テロップ字幕の修正**: 誤認識された漢字の修正、開始・終了時間の微調整、行の追加・削除。
* **🔄 修正して動画＆サムネに再反映**: ボタンを押すと、数秒で動画・音声・サムネイルが即座に再レンダリングされます。

---

### 🧹 ワンクリック・ストレージ容量解放（一括クリーンアップ）
動画ファイルを扱うアプリで問題になりやすい「ディスク容量の圧迫」を完全に解消します。

* **リアルタイム容量表示**: ヘッダー右上に `🧹 一時ファイル: 3.4 GB` のように現在の消費容量を常時表示。
* **ワンクリック全削除**: ヘッダーまたは生成完了画面の `[ 🗑️ 一時ファイルを全削除 ]` ボタンを押すだけで、`uploads/` と `outputs/` の蓄積データを安全に一括削除して容量をゼロに戻せます。

---

### 🛑 ワンクリック・アプリ停止 ＆ ターミナル終了
* 画面右上の **`[ 🛑 アプリ停止 ]`** ボタンを押すことで、バックエンドの Python/Flask サーバープロセスを安全に終了（ターミナル停止）させ、ブラウザウィンドウを閉じます。

---

## 4. 推奨ワークフロー（ステップ・バイ・ステップ操作手順）

### ステップ 1: アプリの起動
ターミナルで以下のコマンドを実行し、ブラウザで `http://localhost:5005` を開きます。
```bash
cd "/Volumes/DTM/applications/Shorts Movie Maker"
python3 app.py
```

### ステップ 2: 動画の読み込み
* 横動画ファイル（MP4, MOV等）を画面中央の点線枠にドラッグ＆ドロップします。
* 0.05秒で実写サムネイルが読み込まれ、8区画グリッド（A〜H）が表示されます。

### ステップ 3: グリッドマッピングの確認・設定
* 「縦動画の分割レイアウト」を選択（例: `上下2分割`）。
* 必要に応じて「⚡ おすすめプリセット」（例: `🎸 ギター2画面 (上:A / 下:F)`）を押すか、プルダウンから好きな区画を選択します。

### ステップ 4: オプション設定
* **1本の長さ**: `15秒`（推奨）
* **生成本数**: `4クリップ`（推奨）
* **音声フック**: 好きな声を選択し、**`[🔊 声を試聴]`** で確認。
* **タイトル文字サイズ**: `特大 (76px)`（推奨）
* **Gemini API Key**: （お持ちの場合は入力。localStorageに自動保存されます）

### ステップ 5: 一括生成の実行
* **「🚀 ショート動画 ＆ サムネを一括生成」** ボタンをクリック。
* プログレスバーが進み、AI解析・切り出し・音声合成・テロップ焼き込み・サムネイル生成が全自動で進行します。

### ステップ 6: プレビュー確認 ＆ 微調整
* 生成完了後、動画の再生プレビューとサムネイル画像が表示されます。
* テロップやバナーを直したい場合は **`[ ✏️ 字幕＆バナー編集 ]`** を開き、修正して **`[ 🔄 修正して動画＆サムネに再反映 ]`** を押します。

### ステップ 7: 一括ダウンロード ＆ 容量解放・終了
1. **`[ 📦 すべて一括ダウンロード (ZIP) ]`** を押して、完成した動画・サムネイル・SRT字幕をまとめて保存します。
2. ダウンロード完了後、**`[ 🗑️ 一時ファイルを全削除 ]`** を押してディスク容量を解放します。
3. 作業を終えるときは、右上の **`[ 🛑 アプリ停止 ]`** を押して終了します。

---

## 5. 各パラメータ・設定項目の詳細

| 設定項目 | 選択肢 / デフォルト | 説明 |
| :--- | :--- | :--- |
| **動画クロップ方式** | `8区画グリッド分割 (推奨)` / `上下ブラー` / `中央クロップ` | 縦動画（9:16）への変換方式。通常は8区画グリッド分割を使用。 |
| **縦動画レイアウト** | `上下2分割`, `上中下3分割`, `4段分割`, `2x2タイル`, `左右2分割` | 縦画面をどのように分割して配置するかを指定。 |
| **ショート動画の長さ** | `10秒`, `15秒 (推奨)`, `20秒`, `30秒` | 切り出す1本あたりの目標尺。15秒が平均視聴維持率に最適。 |
| **生成ショート動画数** | `2` 〜 `6 クリップ` (初期値: `4`) | 1本の元動画から生成するショート動画の本数。 |
| **音声フック読み上げ** | `Style-BERT-VITS2`, `edge-tts`, `なし` | 冒頭のタイトル読み上げ音声。`[🔊 声を試聴]` で事前確認可能。 |
| **タイトル文字サイズ** | `超特大 (84px)`, `特大 (76px 推奨)`, `大 (68px)`, `中 (58px)` | スマホ画面上部のタイトルバナーのフォントサイズ。 |
| **バナー・サムネ配色** | `黄帯×黒`, `赤帯×黒`, `シンプル白枠` | バナーのプレート色および枠線カラー。 |
| **テロップフォント** | `角ゴシック (太字)`, `丸ゴシック`, `明朝体`, `ポップ体` | 画面下部に焼き込むテロップ字幕のフォント。 |

---

## 6. トラブルシューティング ＆ よくある質問 (FAQ)

### Q1. Style-BERT-VITS2 が「⚪ 未起動」と表示されます
* **回答**: Style-BERT-VITS2 はローカルの独立したサーバーです。起動していない場合でも、自動的に **`edge-tts`（高品質クラウド音声）** が併用されますので、そのまま高音質なナレーション音声を生成できます。
* Style-BERT-VITS2 を使用したい場合は、Style-BERT-VITS2 のフォルダでサーバーを起動してください（ポート 5001 / 7860 推奨）。

### Q2. 選択した声と違う声で生成されることはありますか？
* **回答**: `v1.2.6` の改修により、macOS の AirPlay 競合問題を完全に解決しました。生成前に **`[🔊 声を試聴]`** ボタンを押して確認した通りの声で 100% 確実に生成されます。

### Q3. 生成ボタンを押しても反応がありません
* **回答**: ブラウザに古いキャッシュが残っている可能性があります。キーボードの **`Cmd + Shift + R`**（キャッシュを無視して強制リロード）を押してください。

### Q4. 生成された動画の文字が小さく感じます
* **回答**: `v1.2.7` より、タイトル文字サイズがスマホ最適化された **`76px` / `84px`** に強化されました。編集モーダルからフォントサイズを「超特大 (84px)」に変更して再反映をお試しください。

### Q5. ディスク容量が圧迫されてきました
* **回答**: ヘッダー右上の **`[ 🧹 一時ファイル: ○○ GB (全削除) ]`** ボタンを押してください。ダウンロード済みデータに影響を与えることなく、サーバー内の一時ファイルを安全に全削除できます。

---

## 📄 ライセンス ＆ クレジット
* **Shorts Movie Maker** is developed for local video creator workflows.
* Built with Python, Flask, FFmpeg, faster-whisper, edge-tts, Style-BERT-VITS2, and Google Gemini API.
