# Shorts Movie Maker — 要件定義書

**作成日**: 2026-08-14  
**更新日**: 2026-08-14（環境調査結果を反映）  
**ステータス**: Draft v0.2  
**参考資産**: `/Volumes/DTM/applications/lyrics-telop-app/`

---

## 1. プロジェクト概要

長尺の動画ファイル（MP4, MOV 等）をローカルPC上でAI処理し、
SNS Shorts（縦型 9:16）向けのショート動画クリップを**自動生成**するツール。

wayin.ai などのクラウドサービスと同等の機能をローカル・無制限・無料で実現する。  
字幕（テロップ）生成は `lyrics-telop-app` で構築済みのスクリプト資産を最大限に再利用する。

---

## 2. 目標・ゴール

| # | ゴール |
|---|---|
| G1 | 動画ファイルを入力すると、AIがハイライトシーンを自動判定し複数のショート動画候補を出力する |
| G2 | 各ショートには字幕（テロップ）が自動付与される |
| G3 | 縦型（9:16）にリサイズ・クロップされ、そのままYouTube Shorts / TikTok / Reelsに投稿できる |
| G4 | 操作はブラウザUIで完結する（コマンドライン知識不要） |
| G5 | ハイライト判定はGemini APIを使用（字幕テキストのみ送信、動画本体は送信しない） |

---

## 3. 機能要件

### 3-1. 入力

| 入力 | 詳細 |
|---|---|
| 動画ファイル | MP4 / MOV / MKV / WebM（ローカルパス or ブラウザUI上でアップロード） |
| YouTube URL（将来対応） | `yt-dlp` 経由でダウンロードして処理 |
| ハイライト判定モード | `自動（LLM）` / `手動タイムスタンプ指定` |
| 字幕言語 | 日本語 / 英語（Whisperが対応する言語） |
| 出力アスペクト比 | 9:16（縦型）固定 または 1:1 を選択 |

### 3-2. 処理パイプライン（コア）

```
[動画ファイル入力]
       │
       ▼
  ① 音声抽出（FFmpeg）
       │  audio.wav
       ▼
  ② 文字起こし（openai-whisper / キャッシュ済みモデルを使用）
       │  タイムスタンプ付きSRTセグメント
       ▼
  ③ ハイライト判定（Gemini API）
       │  「重要シーン」のタイムスタンプリスト
       ▼
  ④ 動画クリップ切り出し（FFmpeg）
       │  raw_clip_001.mp4 ...
       ▼
  ⑤ 縦型クロップ・リサイズ（FFmpeg, 9:16）
       │  縦型映像
       ▼
  ⑥ 字幕テロップ焼き込み（lyrics-telop-app の video_gen.py 転用）
       │
       ▼
  [ショート動画.mp4 完成]
```

### 3-3. ハイライト判定（③の詳細）

- Whisperで生成したSRTテキストを **Gemini API** に渡す
- プロンプト例：「以下のトランスクリプトから、視聴者が最も興味を持ちそうな箇所を3〜5箇所選び、開始・終了タイムスタンプと理由をJSON形式で返してください」
- **送信するのはテキストのみ**（動画・音声ファイルはローカルから出ない）
- **APIキー未設定時のフォールバック**: 無音区間をシーン境界として均等分割（LLM不使用）

> **Gemini APIの無料枠**: Flash モデルで1分あたり15リクエスト / 1日1500リクエスト。  
> ショート動画1本の生成に使うのは1〜2リクエストなので、通常の用途では無料枠で十分。

### 3-4. 出力

| 出力 | 詳細 |
|---|---|
| ショート動画 | 最大10クリップ（15秒〜90秒）・MP4（H.264 + AAC） |
| 字幕付きか否か | 選択可能（焼き込み or 字幕なし） |
| SRTファイル | 各クリップに対応したSRT字幕ファイルも出力 |
| サムネイル画像 | 各クリップの代表フレーム（PNG）を自動抽出（将来機能） |

---

## 4. 非機能要件

| # | 要件 |
|---|---|
| N1 | **動画はローカル完結**: 動画・音声ファイルは外部送信しない。ハイライト判定テキストのみGemini APIに送信 |
| N2 | **Mac対応**: macOS 12+ (Apple Silicon / Intel) |
| N3 | **GPU対応**: Apple Silicon MPS / CUDA（ある場合）を自動検出して高速化 |
| N4 | **処理速度**: 30分動画から5クリップ抽出まで概ね10分以内（M1 MacBook Air 8GB目安） |
| N5 | **ブラウザUI**: Flask ベース（lyrics-telop-app と同じ構成）でローカルホスト起動 |

---

## 5. 再利用する既存資産（lyrics-telop-app より）

| ファイル | 再利用内容 |
|---|---|
| `core/whisper_sync.py` | `transcribe_audio()` → 音声→SRTタイムライン生成（ほぼそのまま使用可） |
| `core/vad_sync.py` | `export_srt()` → SRTエクスポート / `align_timeline_with_whisper_ai()` → 精密タイムスタンプ調整 |
| `core/video_gen.py` | `generate_video()` → テロップ付き動画生成（9:16対応に設定を追加） |
| `core/text_splitter.py` | `auto_split_timeline()` → 長文字幕の自動分割（そのまま使用可） |
| `app.py` | Flask Webアプリの基本構成を参考に新アプリを構築 |

**新規作成が必要なモジュール:**

| ファイル（予定） | 役割 |
|---|---|
| `core/audio_extractor.py` | FFmpegで音声のみ抽出するユーティリティ |
| `core/highlight_detector.py` | Gemini API呼び出しによるハイライト判定（APIキー未設定時はルールベースでフォールバック） |
| `core/video_splitter.py` | FFmpegを使ったクリップ切り出し・縦型クロップ |
| `app.py` | Flask Webアプリ（メインエントリ） |

---

## 6. 画面構成（UI イメージ）

```
┌──────────────────────────────────────────────┐
│  🎬 Shorts Movie Maker                       │
├──────────────────────────────────────────────┤
│  [1] 動画をアップロード                        │
│      ┌─────────────────────────────────────┐ │
│      │  ここにファイルをドロップ、または選択  │ │
│      └─────────────────────────────────────┘ │
│                                              │
│  [2] 設定                                    │
│      字幕言語:       [ 日本語 ▼ ]             │
│      ハイライト検出: [ 自動(LLM) ▼ ]          │
│      出力形式:       [ 9:16 縦型 ▼ ]          │
│      最大クリップ数: [ 5 ]                    │
│      クリップ長さ:   [ 15 〜 90 ]秒           │
│      字幕:           [ あり ▼ ]              │
│                                              │
│  [3] [ 🚀 ショート生成開始 ]                  │
│                                              │
│  [4] 進捗バー ████████░░ 80%                 │
│      「文字起こし中...」                       │
│                                              │
│  [5] 生成されたクリップ一覧                    │
│      [▶ プレビュー] [⬇ ダウンロード] × n     │
└──────────────────────────────────────────────┘
```

---

## 7. 技術スタック

| 分類 | 技術 | 状況 |
|---|---|---|
| バックエンド | Python 3.11+ / Flask 3.1.3 | ✅ インストール済み |
| 音声認識 | `openai-whisper`（`base` / `small` / `medium` モデル） | ✅ キャッシュ済み |
| LLM（ハイライト判定） | **Gemini API**（`gemini-2.0-flash`）/ APIキー未設定時はルールベース | 要: `pip3 install google-generativeai` |
| 動画処理 | `FFmpeg` v8.0（Homebrew） | ✅ インストール済み |
| 字幕描画 | `Pillow` 12.2.0 + FFmpeg stdin pipe（video_gen.py 転用） | ✅ インストール済み |
| フロントエンド | HTML + Vanilla CSS + JavaScript（fetch API） | 新規作成 |
| パッケージ管理 | `pip3` / `requirements.txt` | ✅ 利用可能 |

**追加インストール必要なもの（合計 ~2MB）:**
```bash
pip3 install google-generativeai
```

---

## 8. ディレクトリ構成（予定）

```
Shorts Movie Maker/
├── 要件定義.md               ← このファイル
├── app.py                    ← Flask メインアプリ
├── requirements.txt          ← 依存パッケージ
├── 起動.command              ← Mac用ワンクリック起動スクリプト
├── core/
│   ├── __init__.py
│   ├── audio_extractor.py    ← [新規] FFmpeg音声抽出
│   ├── whisper_sync.py       ← [転用] lyrics-telop-appより
│   ├── vad_sync.py           ← [転用] lyrics-telop-appより
│   ├── text_splitter.py      ← [転用] lyrics-telop-appより
│   ├── video_gen.py          ← [転用+改修] 9:16対応追加
│   ├── video_splitter.py     ← [新規] クリップ切り出し・縦型クロップ
│   └── highlight_detector.py ← [新規] LLMハイライト判定
├── templates/
│   └── index.html            ← メインUI
├── static/
│   ├── style.css
│   └── app.js
├── uploads/                  ← アップロードされた元動画（一時）
└── outputs/                  ← 生成されたショート動画
```

---

## 9. 開発フェーズ

| フェーズ | 内容 | 優先度 |
|---|---|---|
| **Phase 1** | コアパイプライン：音声抽出 → Whisper文字起こし → FFmpegクリップ切り出し → 縦型変換 | 🔴 高 |
| **Phase 2** | LLMハイライト判定（Gemini API連携） | 🔴 高 |
| **Phase 3** | 字幕テロップ焼き込み（video_gen.py転用） | 🟡 中 |
| **Phase 4** | Flask WebUI（ブラウザ操作） | 🟡 中 |
| **Phase 5** | yt-dlp YouTube URL直接入力対応 | 🟢 低 |
| **Phase 6** | サムネイル自動生成・一括エクスポート | 🟢 低 |

---

## 10. 未決定事項・検討が必要な点

- [ ] Gemini APIキーの管理方法（`.env` ファイル / 環境変数）
- [ ] LLMのプロンプト設計（どういう基準でハイライトを選ぶか？）
- [ ] 縦型クロップ戦略（中央固定 / 顔追跡 / 動き検出）
- [ ] クリップ長さの上限・下限（15秒〜90秒が妥当か？）
- [ ] 字幕フォントサイズ・位置（縦型画面でのUI/UX最適値）
- [ ] 複数クリップの重複を許容するか（同じシーンが複数候補になる場合）
- [x] ~~Ollamaが未インストールの場合の代替手段~~ → Gemini APIを標準採用・決定済み

---

*本ドキュメントは開発着手前の初期要件定義です。実装過程で随時更新します。*
